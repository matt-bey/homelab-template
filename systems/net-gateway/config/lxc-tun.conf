# Proxmox LXC config additions — applied automatically by lxc-create.sh.
# Grants the unprivileged container access to the kernel TUN device
# so Tailscale can manage WireGuard interfaces.

lxc.cgroup2.devices.allow: c 10:200 rwm
lxc.mount.entry: /dev/net/tun dev/net/tun none bind,create=file
